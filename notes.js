/**
 * // creates a collection automatically
 * db.products.insert({name: 'Rubber Duck'})
 * show collections
 * db.products.find()
 * db.products.find({"name" : "Rubber Duck"})
 * db.products. `TAB` // to get a list of commands
 */
